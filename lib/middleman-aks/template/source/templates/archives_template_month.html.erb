---
layout: page
---

<% pages ||= [] %>

<% content_for(:title) { "Archives for #{ Date.new(year, month, 1).strftime("%b %Y") }" } %>

<ul>
<% pages.each do |page| %>
<li><%= link_to(h(page_title(page)), page) %>
<% end%>
</ul>
